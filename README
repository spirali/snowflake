Snowflake is the ultra light static web generator. Instead a large collection of functions and settings, Snowflake offers minimalist codebase that can be easily customize to concrete needs.

Example usage:
$ cd example
$ python ../snowflake.py *.w
See results in directory out/
